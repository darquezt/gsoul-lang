let x = {
  true
  ? Tuple(2, 3, 4)
  : Tuple(1, 2, 3);
};
print 4 + x[1] * 3 < 15;

printEv 1 :: Number![2x + 4y] + x[1];

(fn (x : Number) => fn (y : Number) => x + y)(2)(3);

let f = fn (x: Number![1x]) => (x, x) :: (Number![1x], Number![1x])![2x];

printEv f(2)[1] + 2;
