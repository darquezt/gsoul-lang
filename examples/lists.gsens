let l = fold<rec a. Nil + (Number, a)>(
  inr<Nil>(
    (
      1,
      fold<rec a. Nil + (Number, a)>(
        inl<(Number, rec a. Nil + (Number, a))>(nil)
      )
    )
  )
);

case (unfold(l)) of {
  inl(x) => 0
} {
  inr(x) => 1
};
