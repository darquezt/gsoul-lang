forall x. {
  let Y = fun (f : (Number![1x] -> Number![?x]) -> (Number![1x] -> Number![?x])) {
    let A = fun (x : rec alpha . (alpha -> (Number![1x] -> Number![?x]))) {
      fun (a : Number![1x]) {
        f((unfold(x))(x))(a);
      };
    };

    let B = fun (y : rec alpha . (alpha -> (Number![1x] -> Number![?x]))) {
      fun (b : Number![1x]) {
        f(unfold(y)(y))(b);
      };
    };

    A(fold<rec alpha . (alpha -> (Number![1x] -> Number![?x]))>(B));
  };

  printEv Y;
};
